export const environment = {
  production: true,
  // IMPORTANTE:
  // 1. Pega tu URL de Ngrok ACTUAL (la HTTPS).
  // 2. No pongas slash al final.
  // 3. Apunta a la base de tu API (ej: /api/v1).
  apiUrl: 'https://beholdable-coy-unhieratically.ngrok-free.dev/api/v1'

};

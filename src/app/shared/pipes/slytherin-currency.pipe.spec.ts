import { SlytherinCurrencyPipe } from './slytherin-currency.pipe';

describe('SlytherinCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SlytherinCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
